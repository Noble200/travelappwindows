<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Allva.Desktop.Views.Admin.MenuHamburguesa.CentroAyudaAdminView">

    <Grid RowDefinitions="Auto,*">

        <!-- HEADER -->
        <Border Grid.Row="0"
                Background="#F8F9FA"
                Padding="24,16"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">
            <StackPanel Spacing="4">
                <TextBlock Text="Centro de Ayuda"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#0b5394"/>
                <TextBlock Text="Gestiona el contenido del centro de ayuda del Front Office"
                           FontSize="13"
                           Foreground="#666666"/>
            </StackPanel>
        </Border>

        <!-- CONTENIDO -->
        <Border Grid.Row="1"
                Background="White"
                Padding="24">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                <TextBlock Text="Seccion Centro de Ayuda"
                           FontSize="18"
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Aqui podras gestionar el centro de ayuda del Front Office"
                           FontSize="13"
                           Foreground="#AAAAAA"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

    </Grid>

</UserControl>
